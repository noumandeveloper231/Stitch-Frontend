# Full backend API base (include /api). Must use http:// or https:// — host-only values break on Vercel.
VITE_API_URL=http://localhost:5000/api
# Production example:
# VITE_API_URL=https://your-backend.vercel.app/api
