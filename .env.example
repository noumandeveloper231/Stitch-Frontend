# Full API base including path prefix (e.g. .../api). For Vercel production, include https://
# or the host only — missing scheme is auto-prefixed with https:// (except localhost).
VITE_API_URL=http://localhost:5000/api
